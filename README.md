## Swagger UI

Use the static Swagger UI page in [swagger-ui.html](swagger-ui.html).

Serve the folder with a simple web server so the browser can load `swagger.yaml`:

```bash
python -m http.server 8000
```

Then open:

`http://localhost:8000/swagger-ui.html`
